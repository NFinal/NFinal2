@* Generator: Template
    GeneratePrettyNames : true *@
@functions{
    public NFinal.Compile.Template.App.Models.TipsModel Model { get; set; }
}
using System;
using System.Collections.Generic;
using System.Web;

namespace @{@Model.project}.@{@Model.app}.Models.Tips.@{@Model.database}
{
    public struct @{@Model.table.name}
    {
		@foreach(var field in Model.table.fields){
		@:public bool @{@field.name};
		}
    }
}